import React, { useEffect, useState } from 'react';
import api from '../services/api';
const UserManagement = () => { const [users, setUsers] = useState([]); const [error, setError] = useState(''); useEffect(() => { api.get('/admin/users').then((r) => setUsers(r.data.users || [])).catch((e) => setError(e.response?.data?.message || 'Unable to load users.')); }, []); return <div className="main-content"><h2>User Management</h2><p className="text-muted mb-3">Administrator-only account overview.</p>{error && <div className="notice notice-error">{error}</div>}<div className="table-container"><table className="data-table"><thead><tr><th>Name</th><th>Email</th><th>Role</th><th>Approval</th><th>Joined</th></tr></thead><tbody>{users.map((user) => <tr key={user.id}><td>{user.name}</td><td>{user.email}</td><td>{user.role}</td><td>{user.approval_status}</td><td>{new Date(user.created_at).toLocaleDateString()}</td></tr>)}</tbody></table></div></div>; };
export default UserManagement;
