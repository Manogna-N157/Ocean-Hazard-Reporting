const router = require('express').Router();
const { authenticate, authorize, requireApprovedAuthority } = require('../middleware/authMiddleware');
const controller = require('../controllers/adminController');
router.use(authenticate, requireApprovedAuthority);
router.get('/reports', authorize('Admin'), controller.getAllReports);
router.put('/reports/:id/verify', authorize('Admin'), controller.verifyReport);
router.put('/reports/:id/reject', authorize('Admin'), controller.rejectReport);
router.get('/pending-authorities', authorize('Admin'), controller.getPendingAuthorities);
router.put('/approve-authority/:id', authorize('Admin'), controller.approveAuthority);
router.put('/reject-authority/:id', authorize('Admin'), controller.rejectAuthority);
router.get('/authorities/pending', authorize('Admin'), controller.getPendingAuthorities);
router.put('/authorities/:id/approve', authorize('Admin'), controller.approveAuthority);
router.put('/authorities/:id/reject', authorize('Admin'), controller.rejectAuthority);
router.get('/users', authorize('Admin'), controller.getUsers);
router.put('/users/:id', authorize('Admin'), controller.updateUser);
module.exports = router;
